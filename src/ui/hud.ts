import Phaser from 'phaser';

export function initHUD(_game: Phaser.Game): void {
  // HUD is static HTML in index.html, no extra setup needed
  // This module exists for the architecture pattern and future extensions
}
